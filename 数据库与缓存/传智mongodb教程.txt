传智播客mongodb教程笔记

sun公司: java mysql
后来Oracle收购了sun公司,所以现在java和mysql都是属于Oracle的了.

MariaDB是MySQL的一个分支,是mysql之父做出来的,它的吉祥物是海豹.

在使用方面是和mysql几乎是完全一样的 

端口也是3306

DB2是IBM公司(蓝色巨人)的 

thinkpad之前是IBM的,后来被联想收购了 

sqlite手机用的

非关系型数据库

新的公司往往更倾向使用新的技术,或者新的项目会使用新的技术.

有索引和无索引的区别:
	有索引可以大大提高查询速度,但是会牺牲插入速度,因为会在插入的同时添加索引.

数据库的访问流程： 用户 -> 应用 -> redis或者mongodb（如果没有数据，会从MySQL等持久化数据库中取数据，然后再把数据存储在redis或者mongodb中，最后返回给用户）
如果redis中已经有数据，会直接取出它里面的数据

MongoDB 是一个基于分布式 文件存储的NoSQL数据库
由C++语言编写，运行稳定，性能高	

window下设置mongodb配置文件
	Mongodb配置文件配置：
	第一步：在“D:\Mongodb”目录下新建一个文件，命令方式不限，例如新建一个mongodb.conf

	第二步：在mongodb.conf中配置我们需要的配置项
	关于具体的配置项，我们可以通过bin\mongod --help命令来确定我们需要配置的配置项

	例如我们在mongodb.conf中配置如下：

	dbpath=D:\Mongodb\data  
	logpath=D:\Mongodb\log\Mongodb.log  
	logappend=  
	journal=  
	jsonp=  

	详细说明：
	dbpath代表Mongodb数据库对应的目录，参数对应值为D:\Mongodb\data

	logpath代表日志文件的文件路径，参数值就是文件路径的值

	logappend代表Mongodb的日志信息在日志文件中累加而不是覆盖，没有参数值，所以value为空

	journal代表启动日志，没有参数值，所以value为空
	
	jsonp代表允许通过http方式来访问jsonp格式数据，没有参数值

	通过配置文件来启动服务 ：
	mongod -f D:\Mongodb\mongodb.conf 





































学习进度:  1 